const root=document.documentElement;const bar=document.querySelector('.progress');function setProgress(){if(!bar)return;const h=root.scrollHeight-root.clientHeight;const p=h<=0?0:root.scrollTop/h*100;bar.style.width=p+'%'}setProgress();addEventListener('scroll',setProgress,{passive:true});if(location.pathname.startsWith('/blog/post/')){const s=document.createElement('script');s.src='/assets/math-normalizer.js?v=1.1';document.head.append(s)}const q=document.querySelector('[data-search]');const cards=[...document.querySelectorAll('[data-title]')];if(q){q.addEventListener('input',()=>{const key=q.value.trim().toLowerCase();cards.forEach(card=>{const text=(card.dataset.title+' '+card.dataset.tags+' '+card.textContent).toLowerCase();card.hidden=key&&!text.includes(key)})})}
